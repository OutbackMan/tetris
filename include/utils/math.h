#ifndef __utils_math_h__
#define __utils_math_h__

float G_MATH_MIN_FLOAT(float x, float y);

float G_MATH_MAX_FLOAT(float x, float y);


int G_MATH_INT_MIN(int x, int y);

int G_MATH_INT_MAX(int x, int y);

int G_MATH_INT_MAX_CLAMP(int x, int max);

int G_MATH_INT_MIN_CLAMP(int x, int min);


#endif
